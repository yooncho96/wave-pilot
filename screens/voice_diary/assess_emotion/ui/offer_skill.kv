<OfferSkillScreen>:
    BoxLayout:
        orientation: 'vertical'
        padding: 20
        spacing: 10

        Label:
            id: message_label
            text: ''  # The text will be set in the Python file dynamically

        Button:
            id: try_button
            size_hint: (1, 0.1)
            text: ''  # The text will be set based on the condition in Python

        Button:
            id: later_button
            size_hint: (1, 0.1)
            text: ''  # The text will be set based on the condition in Python
